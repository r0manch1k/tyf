import { createStore } from "vuex";
import profile from "@/stores/modules/profile";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    profile,
  },
});
